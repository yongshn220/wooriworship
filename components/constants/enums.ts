

export enum Routes {
  BOARD = "/board",
  PLAN = "/plan",
  SONG = "/song",
  HOME = "/"
}

export enum Page {
  BOARD,
  PLAN,
  SONG,
  HONE,
}


export enum Param {
  TEAM_ID = "teamId"
}

export enum UserRole {
  'Leader' = 0,
  'Member' = 1
}
