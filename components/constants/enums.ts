

export enum Routes {
  BOARD = "/board",
  PLAN = "/board/plan",
  SONG = "/board/song"
}


export enum SearchParams {
  SONG_BOARD = "song-board"
}

export enum UserRole {
  'Leader' = 0,
  'Member' = 1
}